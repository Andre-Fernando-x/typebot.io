{
  "name": "root-proxy",
  "private": true,
  "scripts": {
    "build": "npm --workspace=landing-page run build",
    "start": "npm --workspace=landing-page run build && npm --workspace=landing-page run preview -- --host 0.0.0.0 --port $PORT"
  },
  "workspaces": ["apps/*", "packages/*"]
}
