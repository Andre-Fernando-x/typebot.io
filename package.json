{
  "name": "root-proxy",
  "private": true,
  "scripts": {
    "build": "npm --workspace=landing-page run build",
    "start": "pnpm --filter=landing-page build && pnpm --filter=landing-page preview --host 0.0.0.0 --port $PORT"
  },
  "workspaces": ["apps/*", "packages/*"]
}
